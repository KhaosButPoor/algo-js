let names = []; // Tableau vide

names.push("Vincent")
names.push("Paul")
names.push("Arthur") // Ajout de valeurs dans le tableau

names.forEach(name => {
  name += "Va a la peche"
  console.log(name);
});